describe('The example spec',  function () {
	'use strict';
	beforeEach(function () {
		//module('myAwesomeProject');
	});

	it('should just run', inject(
		function () {
			expect(true).toBeTruthy();
		}
	));
});
